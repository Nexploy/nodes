{
  "nodes": {
    "set-runner": {
      "name": "Runner di build",
      "description": "Invia il lavoro di build di tutti i nodi successivi a un runner remoto invece che a questo server"
    }
  }
}
