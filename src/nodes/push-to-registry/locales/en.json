{
  "nodes": {
    "push-to-registry": {
      "name": "Push an image to a registry",
      "description": "Push a Docker image to a private or public registry",
      "steps": {
        "push": "Pushing {image}"
      },
      "summary": {
        "pushed": "{count} images · {tag} · {registry}"
      }
    }
  },
  "config": {
    "registriesLoading": "Loading registries...",
    "registryUnavailable": "Registry unavailable"
  }
}
