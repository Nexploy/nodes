{
  "nodes": {
    "build-docker-image": {
      "name": "Build Docker Image",
      "description": "Build a Docker image from a Dockerfile",
      "steps": {
        "buildImage": "Building {image}",
        "dispatchRunner": "Dispatching to {runner}",
        "layer": "Layer {current}/{total}"
      },
      "summary": {
        "built": "{image}",
        "builtWithLayers": "{image} · {layers} layers",
        "builtOnRunner": "{image} · on {runner}"
      }
    }
  },
  "config": {
    "dockerfileFilePath": "File Path",
    "dockerfileFilePathPlaceholder": "Leave empty for repository root",
    "buildImageNamePlaceholder": "Leave empty for auto-generated name"
  }
}
