{
  "nodes": {
    "condition": {
      "name": "Condition",
      "description": "Gate the pipeline on a value from a previous node output",
      "outputTrue": "True",
      "outputFalse": "False"
    }
  },
  "config": {
    "conditionMode": "Operator",
    "conditionModeAndDesc": "All inputs must succeed",
    "conditionModeOrDesc": "At least one must succeed",
    "conditionInputNodes": "Connected input nodes",
    "conditionNoInputs": "No input nodes connected"
  }
}
