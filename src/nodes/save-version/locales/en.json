{
  "nodes": {
    "save-version": {
      "name": "Save Version",
      "description": "Save the current build as a new version",
      "summary": {
        "saved": "v{version} saved"
      }
    }
  },
  "config": {
    "saveVersionInfo": "Saves the current build as a new numbered version using the built image tag and commit info."
  }
}
