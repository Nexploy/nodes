{
  "nodes": {
    "create-container": {
      "name": "Create Container",
      "description": "Create and start a Docker container from an image",
      "steps": {
        "resolveEnv": "Resolving environment",
        "createContainer": "Creating from {image}"
      },
      "summary": {
        "created": "{container} · {ports} ports"
      }
    }
  },
  "config": {
    "createContainerName": "Container Name",
    "createContainerNamePlaceholder": "e.g. my-app (leave empty to auto-generate)",
    "createContainerRestartPolicy": "Restart Policy",
    "createContainerNetwork": "Network",
    "createContainerNetworkPlaceholder": "e.g. my-network (leave empty for default)"
  }
}
